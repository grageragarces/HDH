now lets look at the documentationupdate the database according to the new structure and generator file and add that: 

- the database is no longer a separate branch but a folder in the main branch that isn't uploaded to pypi 
- the database generator files are in database_generators/
- database is in database/
- explain how to add your partitioner to the database generator.py so it can be added to database