{
  "description": "Different strategies for calculating partition capacity",
  "strategies": {
    "ceiling_division": {
      "formula": "(n_qubits + k - 1) // k",
      "description": "Ceiling division - ensures all qubits can fit"
    },
    "floor_division": {
      "formula": "n_qubits // k",
      "description": "Floor division - may require handling remainders"
    },
    "equal": {
      "formula": "n_qubits / k",
      "description": "Exact division - only works when k divides n_qubits"
    },
    "custom": {
      "formula": "user_defined",
      "description": "User-specified capacity per partition"
    }
  }
}